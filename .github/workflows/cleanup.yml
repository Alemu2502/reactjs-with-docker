name: Clean-Up Old Workflows

on:
  schedule:
    - cron: '22 02 * * *'  # Runs at 5:16 AM Ethiopia local time (UTC+3)
  workflow_dispatch:  # Allows manual triggering

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - name: Install GitHub CLI
        run: sudo apt-get install gh

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      - name: Clean up old workflow runs
        run: |
          gh run list --limit 1000 --json databaseId,status,workflowName,workflow_id,createdAt | jq -c '.[] | select(.status == "completed") | .databaseId' | xargs -I {} gh run delete --run-id {}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
